"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.mapTiles = exports.handlePlacement = void 0;
var tslib_1 = require("tslib");
function handlePlacement(tile) {
    var x = tile.x, y = tile.y;
    var size = tile.block.size;
    x -= Math.floor(size / 2 - 0.1);
    y -= Math.floor(size / 2 - 0.1);
    return { x: x, y: y };
}
exports.handlePlacement = handlePlacement;
function mapTiles(schematic) {
    var e_1, _a;
    var width = schematic.width;
    var result = [];
    for (var x = 0; x < width; x++) {
        result[x] = [];
    }
    try {
        for (var _b = tslib_1.__values(schematic.tiles), _c = _b.next(); !_c.done; _c = _b.next()) {
            var tile = _c.value;
            var size = tile.block.size;
            var start = handlePlacement(tile);
            var end = {
                x: start.x + size,
                y: start.y + size,
            };
            for (var x = start.x; x < end.x && x < schematic.width; x++) {
                for (var y = start.y; y < end.y && y < schematic.height; y++) {
                    result[x][y] = tile;
                }
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return result;
}
exports.mapTiles = mapTiles;
//# sourceMappingURL=util.js.map