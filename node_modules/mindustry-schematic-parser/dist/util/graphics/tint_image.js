"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.tintImage = void 0;
var canvas_1 = require("canvas");
function tintImage(image, color, opacity) {
    if (opacity === void 0) { opacity = 0.5; }
    var newCanvas = canvas_1.createCanvas(image.width, image.height);
    var context = newCanvas.getContext('2d');
    context.save();
    context.fillStyle = color;
    context.globalAlpha = opacity;
    context.fillRect(0, 0, context.canvas.width, context.canvas.height);
    context.globalCompositeOperation = 'destination-atop';
    context.globalAlpha = 1;
    context.drawImage(image, 0, 0);
    context.restore();
    return newCanvas;
}
exports.tintImage = tintImage;
//# sourceMappingURL=tint_image.js.map